import 'package:flutter/material.dart';

class Managing extends StatelessWidget {
  const Managing({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: InkWell(
        onTap: () {},
        child: const Text("Hello!"),
      ),
    );
  }
}
